language: generic
sudo: required
services:
    - docker

before_install:
    - docker build -t jsavage/docker-react -f Dockerfile.dev .
    
script:
    - docker run -e CI=true jsavage/docker-react npm run test